Fake News Classification is an NLP project that classifies news as spam or not spam.
The data is pre-processed using nltk library and python.

The ML model is created and trained using LSTM.

The model gives >90% accuracy when tested against a test dataset.
